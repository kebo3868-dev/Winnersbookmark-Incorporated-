import Layout from '../components/Layout';import SectionHeading from '../components/SectionHeading';import MonthlyRotationCard from '../components/MonthlyRotationCard';import { monthlyRotation } from '../data/monthlyRotation';
export default function MonthlyRotation(){return <Layout><section className="max-w-7xl mx-auto px-4 sm:px-6 pt-12"><SectionHeading eyebrow="Monthly Rotation" title="Every Month, New Mentors. Every Day, New Standards." blurb="Update this dashboard monthly from static data now, then connect it to a CMS later."/><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-4">{monthlyRotation.map(i=><MonthlyRotationCard key={i.label} item={i}/>)}</div></section></Layout>}
